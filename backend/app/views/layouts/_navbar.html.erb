<nav class="navbar navbar-expand-sm sticky-top navbar-dark bg-dark flex-md-nowrap p-0 shadow">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_url %>">
      <%= image_tag ("logos/dibujo.svg"), class: "", width: 200%>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if current_page?(root_path) or current_page?(welcome_index_path) %>
            <a class="nav-link active" aria-current="page" href="<%= root_url %>">Inicio</a>
          <% else %>
            <a class="nav-link" aria-current="page" href="<%= root_url %>">Inicio</a>
          <% end %>
        </li>
        <li class="nav-item">
          <% if current_page?(dashboard_path) or current_page?(dashboard_index_path) or current_page?(dashboard_create_patient_path) %>
            <a class="nav-link active" href="<%= dashboard_url %>">Panel de control</a>
          <% else %>
            <a class="nav-link" href="<%= dashboard_url %>">Panel de control</a>
          <% end %>
        </li>
      </ul>
      <div class="d-flex">
        <% if rodauth.logged_in? %>
          <%= link_to "Salir", rodauth.logout_path, method: :post, class: "btn btn-warning" %>
        <% else %>
          <%= link_to "Entrar", rodauth.login_path, class: "btn btn-outline-light me-2" %>
          <%= link_to "Registrarse", rodauth.create_account_path, class: "btn btn-warning" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>